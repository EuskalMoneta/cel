{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-8 offset-md-2 mt-3">
                <div class="row">
                    <div class="col"><h2 class="secondary-color mb-4"> {{"Reconvertir des eusko en €" | trans }} </h2></div>
                </div>

                <div class="row">
                    <div class="col">
                        <div class="mt-2 mb-4 text-semi-bold">
                            Solde disponible : {{ infosUser.solde | number_format(2, ',') }} eusko
                        </div>

                       {{ form(form) }}
                    </div>
                </div>


            </div>
        </div>
    </div>

{% endblock %}

