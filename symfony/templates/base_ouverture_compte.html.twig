{% extends 'base.html.twig' %}

{% if form is defined %}
    {% form_theme form 'bootstrap_4_layout.html.twig' %}
{% endif %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12 col-md-6 offset-md-3">
                <div class="mt-md-4">
                    <h1 class="sur-titre-ouverture">{{ surtitre }}</h1>
                    <div style="width: 80%">
                        <div>{{ "ETAPE" | trans({'%numero_etape%': numero_etape, '%nb_etapes%': nb_etapes}) }}</div>
                        <h1 class="mb-3">{{ titre }}</h1>
                    </div>

                   {% block content %}

                   {% endblock %}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
