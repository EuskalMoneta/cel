{% extends 'base.html.twig' %}


{% block css %}
    <link href="{{ asset('css/chosen.min.css') }}" rel="stylesheet"  type='text/css'>
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-sm-12 col-md-8 offset-md-2 mt-3">
                <div class="row">
                    <div class="col">
                        <div class="mt-2 mb-4">
                            <h3 class="secondary-color mb-0"> {{ "Mon don 3% Eusko" | trans }}</h3>
                            <div class="main-color text-bold threepermain"> {{ montant_don | number_format(2, ',') }} eusko</div>
                            <p class="secondary-color">{{ "depuis le 1er juillet dernier" | trans }}</p>
                        </div>

                        <div class="mt-2 mb-4">
                            <h3 class="secondary-color mb-0"> {{ "L'association que je parraine" | trans }}</h3>
                            <div class="main-color text-bold threepermain">
                                {% if asso1 != '' %}
                                    {{ asso1 }}
                                {% elseif membre.array_options.options_asso_saisie_libre != '' %}
                                    {{ membre.array_options.options_asso_saisie_libre }}
                                {% else %}
                                    Euskal Moneta
                                {% endif %}
                            </div>
                        </div>
<!-- FIXME
                        <div class="mt-2 mb-4">
                            <h3 class="secondary-color mb-0"> {{ "L'association qui bénéficie de mon 3%" | trans }}</h3>
                            <div class="main-color text-bold threepermain">
                                {% if membre.fk_asso2 != '' %}
                                    {{ membre.fk_asso2 }}
                                {% else %}
                                    Euskal Moneta
                                {% endif %}
                            </div>
                        </div>
-->
                        <a class="btn btn-outline-primary mb-4" href="{{ path('app_asso_modifier') }}">{{ "modifier mon association" | trans }} </a>



                    </div>
                </div>

                <div class="row">
                    <div class="col pl-4" style="margin-top: 70px;">
                        {% include 'asso/explication-3-pourcent.html.twig' %}
                    </div>
                </div>

            </div>
        </div>
    </div>

{% endblock %}

